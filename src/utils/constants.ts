const BASE_URL = 'https://pokeapi.co/api/v2/'
const LIMIT_TO_GET = 30
const PATH_URL = `pokemon?limit=${LIMIT_TO_GET}`
const DARK_THEME = 'dark'

export {BASE_URL, PATH_URL, LIMIT_TO_GET, DARK_THEME}
