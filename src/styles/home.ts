import {StyleSheet} from 'react-native'

const homeStyles = StyleSheet.create({
  container: {
    height: '100%',
  },
  loading: {
    alignSelf: 'center',
    marginVertical: 20,
  },
})

export {homeStyles}
