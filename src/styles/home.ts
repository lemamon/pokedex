import {StyleSheet} from 'react-native'

const homeStyles = StyleSheet.create({
  container: {
    flex: 0.5,
    flexWrap: 'wrap',
    flexDirection: 'row',
    paddingTop: 22,
    height: '100%',
  },
  loading: {
    alignSelf: 'center',
    marginVertical: 20,
  },
})

export {homeStyles}
